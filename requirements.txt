# smbsearch tools (the engine itself is C++: search/build.sh) and SMBZero
numpy
stable-retro>=1.0       # the reference emulator: every route is verified against it
gymnasium               # stable-retro's API
Pillow
imageio
imageio-ffmpeg
torch                   # SMBZero policy / value network
tensorboard             # SMBZero training curves
